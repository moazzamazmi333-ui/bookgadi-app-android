name: app
description: "BookGadi Taxi App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  google_maps_flutter: ^2.6.0
  firebase_core: ^3.0.0
  firebase_database: ^11.0.0
  firebase_storage: ^12.0.0
  geolocator: ^11.0.0
  geocoding: ^3.0.0
  file_picker: ^8.0.0
  intl: ^0.19.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.9

flutter:
  uses-material-design: true